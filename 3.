#include <stdio.h>


int main()
{
    char ch; int n;

    printf("请输入一行字符(用回车结束):");
    ch=getchar();
    for(n=0;ch!='\n';)
    {if('a'<=ch && ch<='z'||'A'<=ch && ch<='z')
    n++;
    ch=getchar();

    }
    printf("其中的英文字符个数为%d\n",n);
    return 0;
}
